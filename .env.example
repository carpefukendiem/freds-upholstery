# Serve images from /public instead of the GoHighLevel CDN.
# Run `npm run images:download` first.
NEXT_PUBLIC_LOCAL_IMAGES=false

# Where quote form submissions get delivered. Without this, leads are only logged.
QUOTE_WEBHOOK_URL=
